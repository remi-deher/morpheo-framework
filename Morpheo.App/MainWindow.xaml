<Window x:Class="Morpheo.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Morpheo POS" Height="450" Width="800"
        Background="#F0F2F5" FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🛒" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock x:Name="TxtNodeName" Text="Loading..." FontWeight="Bold" FontSize="16"/>
                    <TextBlock x:Name="TxtStatus" Text="Waiting for peers..." Foreground="Gray"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ListBox x:Name="LstProducts" Grid.Row="1" BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" Padding="10" Margin="0,0,0,8" CornerRadius="5" Width="700">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Grid.Column="1" Text="{Binding Price, StringFormat={}{0:C}}" Foreground="#2E7D32" FontWeight="Bold"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Content="+ NOUVEAU PRODUIT (Sync)" 
                Height="50" Background="#1A73E8" Foreground="White" 
                FontSize="14" FontWeight="Bold" BorderThickness="0"
                Click="BtnAddProduct_Click" Margin="0,20,0,0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>